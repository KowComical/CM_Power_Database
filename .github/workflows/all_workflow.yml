name: all_workflow
'on': [push]
  #schedule:
    #- cron: 0 22 * * *
jobs:
  iea_bot:
    runs-on: ubuntu-latest
    steps:
      - name: check_code
        uses: actions/checkout@master
      - name: set up Python 3.8
        uses: actions/setup-Python@v2
        with:
          python-version: '3.8'
      - name: install requirements
        run: |
          pip install pandas
          pip install requests
      - name: process data
        run: python ./code/global_code/iea_workflow.py
      - name: commit and push if it changed
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          find ./ -name ".pyc"　|xargs rm -rf
          git add -A
          timestamp=$(date -u)
          git config pull.rebase false
          git commit -m "Latest data: ${timestamp}" || exit 0
          git pull
          git push
  us_bot:
    runs-on: ubuntu-latest
    steps:
      - name: check_code
        uses: actions/checkout@master
      - name: set up Python 3.8
        uses: actions/setup-Python@v2
        with:
          python-version: '3.8'
      - name: install requirements
        run: |
          pip install pandas
          pip install requests
          pip install tqdm
          pip install matplotlib
      - name: process data
        run: python ./code/n_america/us/us_workflow.py
      - name: commit and push if it changed
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          find ./ -name ".pyc"　|xargs rm -rf
          git add -A
          timestamp=$(date -u)
          git config pull.rebase false
          git commit -m "Latest data: ${timestamp}" || exit 0
          git pull
          git push
  brazil_bot:
    runs-on: ubuntu-latest
    steps:
      - name: check_code
        uses: actions/checkout@master
      - name: set up Python 3.8
        uses: actions/setup-Python@v2
        with:
          python-version: '3.8'
      - name: install requirements
        run: |
          pip install pandas
          pip install requests
          pip install matplotlib
          pip install jsonpath
          pip install requests_toolbelt
      - name: process data
        run: python ./code/s_america/brazil/brazil_workflow.py
      - name: commit and push if it changed
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          find ./ -name ".pyc"　|xargs rm -rf
          git add -A
          timestamp=$(date -u)
          git config pull.rebase false
          git commit -m "Latest data: ${timestamp}" || exit 0
          git pull
          git push
  eu_bot:
    runs-on: ubuntu-latest
    steps:
      - name: check_code
        uses: actions/checkout@master
      - name: set up Python 3.8
        uses: actions/setup-Python@v2
        with:
          python-version: '3.8'
      - name: install requirements
        run: |
          pip install pandas
          pip install requests
          pip install tqdm
          pip install matplotlib
          pip install bs4
      - name: process data
        run: python ./code/europe/eu27_uk/eu_workflow.py
      - name: commit and push if it changed
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          find ./ -name ".pyc"　|xargs rm -rf
          git add -A
          timestamp=$(date -u)
          git config pull.rebase false
          git commit -m "Latest data: ${timestamp}" || exit 0
          git pull
          git push
  india_bot:
    runs-on: ubuntu-latest
    steps:
      - name: check_code
        uses: actions/checkout@master
      - name: set up Python 3.8
        uses: actions/setup-Python@v2
        with:
          python-version: '3.8'
      - name: install requirements
        run: |
          pip install pandas
          pip install requests
          pip install tqdm
          pip install matplotlib
          pip install pdfplumber
          pip install bs4
          pip install lxml
      - name: process data
        run: python ./code/asian/india/india_workflow.py
      - name: commit and push if it changed
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          find ./ -name ".pyc"　|xargs rm -rf
          git add -A
          timestamp=$(date -u)
          git config pull.rebase false
          git commit -m "Latest data: ${timestamp}" || exit 0
          git pull
          git push
  japan_bot:
    runs-on: ubuntu-latest
    steps:
      - name: check_code
        uses: actions/checkout@master
      - name: set up Python 3.8
        uses: actions/setup-Python@v2
        with:
          python-version: '3.8'
      - name: install requirements
        run: |
          pip install pandas
          pip install requests
          pip install tqdm
          pip install matplotlib
          pip install bs4
      - name: process data
        run: python ./code/asian/japan/japan_workflow.py
      - name: commit and push if it changed
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          find ./ -name ".pyc"　|xargs rm -rf
          git add -A
          timestamp=$(date -u)
          git config pull.rebase false
          git commit -m "Latest data: ${timestamp}" || exit 0
          git pull
          git push
  russia_bot:
    runs-on: ubuntu-latest
    steps:
      - name: check_code
        uses: actions/checkout@master
      - name: set up Python 3.8
        uses: actions/setup-Python@v2
        with:
          python-version: '3.8'
      - name: install requirements
        run: |
          pip install pandas
          pip install matplotlib
          pip install requests
      - name: process data
        run: python ./code/europe/russia/russia_workflow.py
      - name: commit and push if it changed
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          find ./ -name ".pyc"　|xargs rm -rf
          git add -A
          timestamp=$(date -u)
          git config pull.rebase false
          git commit -m "Latest data: ${timestamp}" || exit 0
          git pull
          git push       
  china_bot:
    runs-on: ubuntu-latest
    steps:
      - name: check_code
        uses: actions/checkout@master
      - name: set up Python 3.8
        uses: actions/setup-Python@v2
        with:
          python-version: '3.8'
      - name: install requirements
        run: |
          pip install pandas
          pip install matplotlib
          pip install requests
      - name: process data
        run: python ./code/asian/china/china_workflow.py
      - name: commit and push if it changed
        run: |
          git config user.name "${GITHUB_ACTOR}"
          git config user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          find ./ -name ".pyc"　|xargs rm -rf
          git add -A
          timestamp=$(date -u)
          git config pull.rebase false
          git commit -m "Latest data: ${timestamp}" || exit 0
          git pull
          git push     
